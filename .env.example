# Copy to .env and adjust if needed. Defaults work out of the box.

MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin123

RAW_BUCKET=raw
BRONZE_BUCKET=bronze
SILVER_BUCKET=silver
QUARANTINE_BUCKET=quality-quarantine

AIRFLOW_UID=50000
